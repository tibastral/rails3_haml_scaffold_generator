%h1= t '.title'
%p
  = link_to t('back'), :<%= plural_name %>
  = link_to t('edit'), [:edit, @<%= singular_name %>]

%dl
<% for attribute in attributes -%>
  %dt= <%= class_name %>.human_attribute_name :<%= attribute.name %>
  %dd= @<%= singular_name %>.<%= attribute.name %>
<% end -%>
